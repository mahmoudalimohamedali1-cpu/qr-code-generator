# 🕐 نظام الحضور والانصراف | Attendance System

نظام متكامل لإدارة حضور وانصراف الموظفين مع دعم الموقع الجغرافي (Geofencing) وكشف المواقع الوهمية.

## 📋 المحتويات

- [الوصف العام](#الوصف-العام)
- [المميزات](#المميزات)
- [التقنيات المستخدمة](#التقنيات-المستخدمة)
- [هيكل المشروع](#هيكل-المشروع)
- [التثبيت والتشغيل](#التثبيت-والتشغيل)
- [API Documentation](#api-documentation)
- [حسابات الاختبار](#حسابات-الاختبار)

## 📝 الوصف العام

نظام حضور وانصراف موجّه للشركات التي لديها موظفين في فروع/مواقع مختلفة. يتيح النظام:

- تسجيل الحضور والانصراف باستخدام الموقع الجغرافي
- التحقق من تواجد الموظف داخل نطاق الشركة (Geofencing)
- كشف محاولات استخدام المواقع الوهمية (Fake GPS)
- إرسال إشعارات فورية للتأخير والانصراف المبكر
- لوحة تحكم شاملة للأدمن

## ✨ المميزات

### تطبيق الموبايل (Flutter)
- ✅ تسجيل الحضور والانصراف مع GPS
- ✅ التحقق من الموقع (Geofencing)
- ✅ كشف Mock Location
- ✅ سجل الحضور اليومي والشهري
- ✅ طلب الإجازات والأذونات
- ✅ الإشعارات الفورية
- ✅ دعم اللغة العربية والإنجليزية
- ✅ الوضع الليلي والنهاري

### لوحة تحكم الأدمن
- ✅ إدارة الموظفين (CRUD)
- ✅ إدارة الفروع والأقسام
- ✅ إعدادات مواعيد العمل
- ✅ تقارير الحضور التفصيلية
- ✅ تصدير التقارير (Excel/PDF)
- ✅ إشعارات التأخير والانصراف المبكر
- ✅ سجل التدقيق (Audit Log)

### Backend API
- ✅ RESTful API موثق
- ✅ JWT Authentication مع Refresh Tokens
- ✅ Role-Based Access Control
- ✅ Geofencing validation
- ✅ Push Notifications (FCM)
- ✅ تكامل مع الرواتب (Payroll Ready)

## 🛠 التقنيات المستخدمة

### Backend
- **Framework:** NestJS (Node.js)
- **Database:** PostgreSQL
- **ORM:** Prisma
- **Authentication:** JWT + Passport
- **Documentation:** Swagger/OpenAPI
- **Notifications:** Firebase Cloud Messaging

### Mobile App
- **Framework:** Flutter 3.x
- **State Management:** BLoC
- **Architecture:** Clean Architecture
- **Location:** Geolocator
- **Storage:** Flutter Secure Storage + Hive
- **DI:** GetIt

### Web Admin (اختياري)
- Flutter Web أو React/Vue

## 📁 هيكل المشروع

```
attendance-system/
├── backend/                 # NestJS Backend
│   ├── prisma/             # Database schema & migrations
│   ├── src/
│   │   ├── common/         # Shared modules (Prisma, etc.)
│   │   ├── modules/
│   │   │   ├── auth/       # Authentication
│   │   │   ├── users/      # User management
│   │   │   ├── branches/   # Branches & departments
│   │   │   ├── attendance/ # Check-in/out
│   │   │   ├── leaves/     # Leave requests
│   │   │   ├── notifications/
│   │   │   ├── reports/    # Reports & exports
│   │   │   ├── audit/      # Audit logs
│   │   │   └── settings/   # System settings
│   │   └── main.ts
│   └── package.json
│
├── mobile/                  # Flutter Mobile App
│   ├── lib/
│   │   ├── core/           # Core utilities
│   │   │   ├── di/         # Dependency injection
│   │   │   ├── network/    # API client
│   │   │   ├── services/   # Location, Storage, Notifications
│   │   │   ├── theme/      # App theme
│   │   │   ├── router/     # Navigation
│   │   │   └── l10n/       # Localization
│   │   └── features/       # Feature modules
│   │       ├── auth/
│   │       ├── home/
│   │       ├── attendance/
│   │       ├── leaves/
│   │       ├── notifications/
│   │       ├── profile/
│   │       └── settings/
│   └── pubspec.yaml
│
└── web-admin/              # Web Admin Dashboard
```

## 🚀 التثبيت والتشغيل

### المتطلبات
- Node.js 18+
- PostgreSQL 14+
- Flutter 3.16+
- Android Studio / Xcode

### 1. إعداد Backend

```bash
cd backend

# تثبيت الحزم
npm install

# نسخ ملف البيئة
cp .env.example .env

# تعديل إعدادات قاعدة البيانات في .env
# DATABASE_URL="postgresql://user:password@localhost:5432/attendance_db"

# تشغيل migrations
npx prisma migrate dev

# تشغيل seed data
npm run prisma:seed

# تشغيل الخادم
npm run start:dev
```

الخادم سيعمل على: `http://localhost:3000`
التوثيق على: `http://localhost:3000/api/docs`

### 2. إعداد تطبيق Flutter

```bash
cd mobile

# تثبيت الحزم
flutter pub get

# تشغيل التطبيق
flutter run
```

### 3. إعدادات Firebase (للإشعارات)

1. أنشئ مشروع Firebase جديد
2. أضف تطبيق Android/iOS
3. انسخ `google-services.json` إلى `mobile/android/app/`
4. انسخ `GoogleService-Info.plist` إلى `mobile/ios/Runner/`
5. أضف مفاتيح Firebase في `.env`

## 📖 API Documentation

بعد تشغيل الخادم، يمكنك الوصول للتوثيق التفاعلي على:

```
http://localhost:3000/api/docs
```

### Endpoints الرئيسية

| Method | Endpoint | Description |
|--------|----------|-------------|
| POST | `/api/v1/auth/login` | تسجيل الدخول |
| POST | `/api/v1/auth/refresh` | تجديد التوكن |
| POST | `/api/v1/attendance/check-in` | تسجيل الحضور |
| POST | `/api/v1/attendance/check-out` | تسجيل الانصراف |
| GET | `/api/v1/attendance/history` | سجل الحضور |
| POST | `/api/v1/leaves` | طلب إجازة |
| GET | `/api/v1/reports/dashboard` | إحصائيات لوحة التحكم |

## 🔐 حسابات الاختبار

| الدور | البريد | كلمة المرور |
|-------|--------|-------------|
| Admin | admin@company.com | admin123 |
| Manager | manager@company.com | manager123 |
| Employee | employee1@company.com | employee123 |

## ⚙️ إعدادات مهمة

### إعدادات الفرع
يمكن تعديل الإعدادات التالية من لوحة التحكم:
- موقع الفرع (Latitude, Longitude)
- نصف قطر الحضور (Geofence Radius)
- وقت بداية ونهاية الدوام
- فترة السماح للتأخير
- فترة السماح للحضور المبكر
- أيام العمل

### قواعد الحضور
- **التأخير:** إذا تجاوز الموظف وقت بداية الدوام + فترة السماح
- **الانصراف المبكر:** إذا انصرف قبل وقت نهاية الدوام
- **الحضور المبكر:** لا يُسمح بالحضور قبل (وقت البداية - فترة السماح للحضور المبكر)

## 📱 لقطات الشاشة

### تطبيق الموبايل
- شاشة تسجيل الدخول
- الشاشة الرئيسية مع أزرار الحضور/الانصراف
- سجل الحضور
- طلبات الإجازة
- الملف الشخصي

### لوحة التحكم
- لوحة الإحصائيات
- إدارة الموظفين
- إدارة الفروع
- التقارير

## 🔒 الأمان

- تشفير كلمات المرور باستخدام bcrypt
- JWT مع انتهاء صلاحية قصيرة + Refresh Token
- التحقق من الموقع عند كل عملية حضور/انصراف
- كشف Mock Location
- تسجيل جميع المحاولات المشبوهة
- Role-Based Access Control
- Audit Log لجميع العمليات المهمة

## 📊 التكامل مع الرواتب

النظام جاهز للتكامل مع أنظمة الرواتب من خلال:
- حساب دقائق التأخير
- حساب دقائق الانصراف المبكر
- حساب الساعات الإضافية
- أيام الغياب
- API لجلب ملخص الرواتب

## 🤝 المساهمة

نرحب بالمساهمات! يرجى قراءة دليل المساهمة قبل إرسال Pull Request.

## 📄 الرخصة

MIT License

---

تم التطوير بـ ❤️ لتسهيل إدارة الحضور والانصراف

