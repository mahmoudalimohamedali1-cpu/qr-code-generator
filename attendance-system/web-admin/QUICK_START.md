# ⚡ دليل البدء السريع

## 🚀 تشغيل النظام بالكامل (3 خطوات)

### 1️⃣ شغّل Backend

```bash
cd ~/attendance-system/backend
npm run start:dev
```

**✅ يجب أن ترى:**
```
🚀 Server running on http://localhost:3000
📚 API Documentation: http://localhost:3000/api/docs
```

### 2️⃣ شغّل Dashboard

```bash
cd ~/attendance-system/web-admin
npm run dev
```

**✅ يجب أن ترى:**
```
  VITE v5.x.x  ready in xxx ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
```

### 3️⃣ افتح المتصفح

```
http://localhost:5173
```

---

## 🔐 تسجيل الدخول

- **البريد:** `admin@company.com`
- **كلمة المرور:** `admin123`

---

## ✅ التحقق من أن كل شيء يعمل

### Backend:
```bash
curl http://localhost:3000/health
```
يجب أن ترى: `{"status":"ok",...}`

### Dashboard:
افتح `http://localhost:5173` - يجب أن ترى صفحة تسجيل الدخول

### إذا ظهرت رسالة "لا يوجد اتصال بالخادم":

1. ✅ تأكد أن Backend يعمل (Terminal 1)
2. ✅ تحقق من `http://localhost:3000/health`
3. ✅ اضغط "إعادة المحاولة" في Dashboard
4. ✅ أعد تحميل الصفحة (F5)

---

## 🎯 بعد تسجيل الدخول

ستظهر لك:
- ✅ لوحة التحكم الرئيسية
- ✅ إحصائيات مباشرة
- ✅ قائمة جانبية للتنقل

---

## 📝 ملاحظات مهمة

- **Backend** يجب أن يعمل دائماً قبل Dashboard
- **PostgreSQL** يجب أن يعمل (عادة يعمل تلقائياً)
- إذا توقف Backend، Dashboard سيعرض رسالة خطأ واضحة

---

**🎉 جاهز للاستخدام!**

