# 🔐 لوحة تحكم نظام الحضور والانصراف - Dashboard

لوحة تحكم ويب متقدمة وآمنة لإدارة نظام الحضور والانصراف.

## 🛡️ ميزات الأمان المتقدمة

### 1. **تشفير البيانات**
- تشفير Tokens باستخدام AES-256
- تشفير بيانات المستخدم في LocalStorage
- تشفير Cookies

### 2. **حماية من الهجمات**
- ✅ **XSS Protection** - تنظيف المدخلات
- ✅ **CSRF Protection** - CSRF tokens
- ✅ **Rate Limiting** - منع Brute Force
- ✅ **JWT Validation** - التحقق من صحة Tokens
- ✅ **Secure Headers** - Security headers في كل طلب

### 3. **إدارة الجلسات**
- JWT Access Tokens (15 دقيقة)
- Refresh Tokens (7 أيام)
- Auto-refresh عند انتهاء الصلاحية
- Logout آمن من جميع الأجهزة

### 4. **Role-Based Access Control (RBAC)**
- حماية المسارات حسب الدور
- ADMIN > MANAGER > EMPLOYEE
- التحقق من الصلاحيات في كل طلب

## 🚀 التشغيل

```bash
cd web-admin

# تثبيت الحزم
npm install

# تشغيل في وضع التطوير
npm run dev

# بناء للإنتاج
npm run build

# معاينة البناء
npm run preview
```

اللوحة ستعمل على: `http://localhost:5173`

## 📋 الحسابات

| الدور | البريد | كلمة المرور |
|-------|--------|-------------|
| Admin | admin@company.com | admin123 |
| Manager | manager@company.com | manager123 |

## 🔧 الإعدادات

### ملف `.env`:
```env
VITE_API_BASE_URL=http://localhost:3000/api/v1
VITE_ENCRYPTION_KEY=your-secret-encryption-key
```

**⚠️ مهم:** غير `VITE_ENCRYPTION_KEY` في الإنتاج!

## 🏗️ البنية المعمارية

```
src/
├── components/      # مكونات قابلة لإعادة الاستخدام
│   ├── auth/        # مكونات المصادقة
│   └── layout/      # مكونات التخطيط
├── pages/           # صفحات التطبيق
├── services/        # خدمات API والأمان
├── store/           # إدارة الحالة (Zustand)
├── types/           # تعريفات TypeScript
├── config/          # إعدادات التطبيق
└── theme.ts         # إعدادات Material-UI
```

## 🔐 الأمان

### Security Headers
- `X-Content-Type-Options: nosniff`
- `X-Frame-Options: DENY`
- `X-XSS-Protection: 1; mode=block`
- `Content-Security-Policy`
- `Referrer-Policy: strict-origin-when-cross-origin`

### Token Management
- Tokens مشفرة في Cookies
- Backup في SessionStorage (مشفر)
- Auto-refresh عند انتهاء الصلاحية
- Logout آمن من جميع الأجهزة

### Rate Limiting
- 5 محاولات لكل 15 دقيقة
- منع Brute Force attacks
- رسائل واضحة للمستخدم

## 📱 Responsive Design
- يعمل على جميع الأجهزة
- Mobile-first approach
- Sidebar قابل للطي على الموبايل

## 🎨 UI/UX
- Material-UI (MUI) v5
- تصميم عربي RTL
- Dark mode ready
- Animations سلسة

## 📊 الميزات

- ✅ Dashboard مع إحصائيات مباشرة
- ✅ إدارة المستخدمين (قريباً)
- ✅ إدارة الحضور (قريباً)
- ✅ إدارة الفروع (قريباً)
- ✅ إدارة الإجازات (قريباً)
- ✅ التقارير (قريباً)
- ✅ الإعدادات (قريباً)

## 🔄 التكامل مع Backend

اللوحة متكاملة بالكامل مع Backend API:
- جميع الـ endpoints جاهزة
- Error handling متقدم
- Retry logic للطلبات الفاشلة
- Loading states

## 🚨 ملاحظات الأمان

1. **في الإنتاج:**
   - استخدم HTTPS فقط
   - غير `VITE_ENCRYPTION_KEY`
   - فعّل CORS بشكل صحيح
   - استخدم Environment Variables آمنة

2. **التشفير:**
   - جميع البيانات الحساسة مشفرة
   - Tokens مشفرة قبل التخزين
   - لا توجد بيانات حساسة في Console

3. **التحقق:**
   - جميع المدخلات يتم التحقق منها
   - Zod schemas للـ validation
   - TypeScript للـ type safety

---

تم التطوير بـ ❤️ مع التركيز على الأمان والجودة
