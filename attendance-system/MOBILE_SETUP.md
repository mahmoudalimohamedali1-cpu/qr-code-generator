# إعداد التطبيق للجوال الحقيقي

## ✅ ما تم إصلاحه:

1. ✅ تحديث API URL إلى: `http://192.168.1.17:3000/api/v1`
2. ✅ تحديث السيرفر لاستقبال الطلبات من الشبكة (0.0.0.0)
3. ✅ تفعيل CORS للسماح بالاتصال من الموبايل

## 📋 الخطوات:

### 1. تشغيل السيرفر
```bash
cd backend
npm run start:dev
```

**مهم**: تأكد أن السيرفر يعمل على `http://0.0.0.0:3000`

### 2. التأكد من الاتصال
- تأكد أن الموبايل والكمبيوتر على نفس شبكة WiFi
- جرب فتح هذا الرابط من الموبايل:
  ```
  http://192.168.1.17:3000/api/v1
  ```
  يجب أن ترى رسالة خطأ (هذا طبيعي، يعني الاتصال يعمل)

### 3. بناء APK جديد
```bash
cd mobile
flutter clean
flutter pub get
flutter build apk
```

### 4. تثبيت APK على الموبايل
- انسخ الملف: `build/app/outputs/flutter-apk/app-release.apk`
- ثبت APK على الموبايل
- جرّب تسجيل الدخول

## 🔧 إذا لم يعمل:

### تحقق من IP الكمبيوتر:
```bash
# Mac/Linux
ifconfig | grep "inet " | grep -v 127.0.0.1

# Windows
ipconfig
```

### إذا تغير IP الكمبيوتر:
1. افتح: `mobile/lib/core/config/app_config.dart`
2. غيّر السطر 13 إلى IP الجديد:
   ```dart
   defaultValue: 'http://YOUR_NEW_IP:3000/api/v1',
   ```
3. أعد بناء APK

### تحقق من Firewall:
- تأكد أن Firewall لا يمنع المنفذ 3000
- على Mac: System Settings > Network > Firewall

## 📱 ملاحظات:
- IP `192.168.1.17` هو IP الكمبيوتر الحالي
- إذا غيرت الشبكة، قد يتغير IP
- تأكد دائماً أن الموبايل والكمبيوتر على نفس WiFi
